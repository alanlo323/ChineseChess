<Window x:Class="ChineseChess.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:ChineseChess.WPF.Views"
        xmlns:vm="clr-namespace:ChineseChess.WPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="Chinese Chess AI" Height="700" Width="900" MinWidth="800" MinHeight="600"
        Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Board Area (Scaled) -->
        <Viewbox Grid.Row="0" Grid.Column="0" Margin="10" Stretch="Uniform">
            <views:ChessBoardView DataContext="{Binding ChessBoard}"/>
        </Viewbox>

        <!-- Control Panel -->
        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" BorderBrush="Gray" BorderThickness="1,0,0,0" Background="#FAFAFA">
            <views:ControlPanelView DataContext="{Binding ControlPanel}"/>
        </Border>

        <!-- Analysis Panel -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="10" Background="#F0F0F0">
            <StackPanel>
                <TextBlock Text="Analysis:" FontWeight="Bold"/>
                <TextBlock Text="{Binding AnalysisText}" TextWrapping="Wrap" FontFamily="Consolas"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
