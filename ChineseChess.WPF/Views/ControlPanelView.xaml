<UserControl x:Class="ChineseChess.WPF.Views.ControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ChineseChess.WPF.ViewModels"
             xmlns:enums="clr-namespace:ChineseChess.Application.Enums;assembly=ChineseChess.Application"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ControlPanelViewModel}"
             Width="250">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="GameModeEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:GameMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    
    <StackPanel Margin="10">
        <TextBlock Text="Chinese Chess" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <TextBlock Text="{Binding StatusMessage}" Foreground="Blue" TextWrapping="Wrap" Margin="0,0,0,10"/>
        
        <GroupBox Header="Settings">
            <StackPanel Margin="5">
                <TextBlock Text="Mode:"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource GameModeEnum}}" SelectedItem="{Binding SelectedMode}" Margin="0,5"/>
                
                <TextBlock Text="Difficulty (Depth):"/>
                <Slider Minimum="1" Maximum="10" Value="{Binding SearchDepth}" AutoToolTipPlacement="TopLeft" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <Button Content="Start Game" Command="{Binding StartGameCommand}" Margin="0,10" Padding="5"/>
        <Button Content="Undo" Command="{Binding UndoCommand}" Margin="0,5" Padding="5"/>
        <Button Content="Hint" Command="{Binding HintCommand}" Margin="0,5" Padding="5"/>
        
    </StackPanel>
</UserControl>
