<UserControl x:Class="ChineseChess.WPF.Views.NnueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ChineseChess.WPF.ViewModels"
             xmlns:views="clr-namespace:ChineseChess.WPF.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:NnueViewModel}">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,8">

            <!-- ─── NNUE 模型管理 ────────────────────────────────────── -->
            <TextBlock Text="NNUE 模型管理"
                       Style="{StaticResource SectionTitleStyle}"
                       Margin="0,0,0,6" />
            <Border BorderBrush="{StaticResource WoodDarkBrush}"
                    BorderThickness="1"
                    CornerRadius="3"
                    Background="{StaticResource PanelBackgroundBrush}"
                    Padding="8,6"
                    Margin="0,0,0,8">
                <views:LoadedNnueModelListView DataContext="{Binding LoadedNnueModelList}" />
            </Border>

            <Separator Margin="0,0,0,8" />

            <!-- ─── 本機訓練 ─────────────────────────────────────────── -->
            <views:NnueTrainingView DataContext="{Binding Training}" />

        </StackPanel>
    </ScrollViewer>
</UserControl>
